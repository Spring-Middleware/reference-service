server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: ${SERVER_CONTEXT_PATH:/catalog}

spring:
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/catalog}
      uuid-representation: standard

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    io.github.spring.middleware: ${LOG_LEVEL_PACKAGE:DEBUG}
    org.springframework.security: TRACE
    org.springframework.security.web: TRACE
    root: ${LOG_LEVEL_ROOT:ERROR}

middleware:
  log:
    apiKey: ${LOG_API_KEY}
    request:
      enabled: ${LOG_REQUEST_ENABLED:true}
    response:
      enabled: ${LOG_RESPONSE_ENABLED:true}
    responseTime:
      enabled: ${LOG_RESPONSE_TIME_ENABLED:false}
    exclude:
      urlPatterns:
        - /api-docs/**
        - /swagger-ui.html
        - /swagger-ui/**

  client:
    registryEndpoint: ${REGISTRY_ENDPOINT:http://localhost:8080/registry}

  resourceRegister:
    clusterName: ${RESOURCE_CLUSTER_NAME:catalog}

  publicServer:
    # In Docker the public hostname will be the service name `registry` (or set via env)
    host: ${PUBLIC_SERVER_HOST:localhost}
    port: ${PUBLIC_SERVER_PORT:8080}

  graphql:
    enabled: ${GRAPHQL_ENABLED:false}

#  security:
#    type: BASIC_AUTH
#    public-path:
#      - /api-docs/**
#      - /swagger-ui.html
#      - /swagger-ui/**
#
#    protected-paths:
#      - enabled: true
#        path: /api/*/catalogs/**
#        methods: [ GET ]
#        allowed-roles: [ GET_CATALOG, ADMIN ]
#
#    basic:
#      credentials:
#        username: ${SECURITY_USERNAME}
#        password: ${SECURITY_PASSWORD}
#        roles: [ GET_CATALOG, ADMIN ]

#  security:
#    type: JWT
#    public-path:
#      - /api-docs/**
#      - /swagger-ui.html
#      - /swagger-ui/**
#
#    protected-paths:
#      - enabled: true
#        path: /api/*/catalogs/**
#        methods: [ GET ]
#        allowed-roles: [ GET_CATALOG, ADMIN ]
#
#    jwt:
#      secret: ${JWT_SECRET:dfwzsdzwh823zebdwdz772632gdsbddfr4}

  security:
    type: OIDC
    public-path:
      - /api-docs/**
      - /swagger-ui.html
      - /swagger-ui/**

    protected-paths:
      - enabled: true
        path: /api/*/catalogs/*/products
        methods: [ GET ]
        allowed-roles: [ LIST_PRODUCTS, ADMIN ]

      - enabled: true
        path: /api/*/catalogs/**
        methods: [ GET ]
        allowed-roles: [ GET_CATALOG, ADMIN ]

    oidc:
      issuer-uri: ${OIDC_ISSUER_URI:http://localhost:8081/realms/spring-middleware}
      jwk-set-uri: ${OIDC_JWK_SET_URI:http://keycloak:8080/realms/spring-middleware/protocol/openid-connect/certs}
      authorities-claim-path: ${OIDC_AUTHORITIES_CLAIM_PATH:$.resource_access.catalog-service.roles[*]}

#  security:
#    type: API_KEY
#    public-path:
#      - /api-docs/**
#      - /swagger-ui.html
#      - /swagger-ui/**
#
#    protected-paths:
#      - enabled: true
#        path: /api/*/catalogs/*/products
#        methods: [ GET ]
#        allowed-roles: [ LIST_PRODUCTS, ADMIN ]
#
#      - enabled: true
#        path: /api/*/catalogs/**
#        methods: [ GET ]
#        allowed-roles: [ GET_CATALOG, ADMIN ]
#
#    api-key:
#        header-name: X-API-KEY
#        credentials:
#          - key: ${API_KEY_CATALOG_SERVICE:default-api-key}
#            enabled: true
#            roles: [ GET_CATALOG, LIST_PRODUCTS ]

  errors:
    CATALOG_NOT_FOUND: 404

redisson:
  # In Docker compose Redis service should be named `redis`
  address: ${REDIS_ADDRESS:redis://localhost:6379}
  database: ${REDIS_DATABASE:0}




